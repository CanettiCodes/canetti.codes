<!doctype html>
<html lang="en">
    <head>
        {% include meta %}
        {% include socialmeta %}
    </head>
    <body class="{{ pageClass }}">
      <div class="wrapper"> 
        {% include nav %}
        {{ content }}
        {% include cookie %}
        {% include footer %}
      </div>
    </body>
</html>
